name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: |
          echo "=================================="
          echo " Release workflow - Coming Soon"
          echo "=================================="