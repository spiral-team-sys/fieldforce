name: Revopush

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: |
          echo "=================================="
          echo " Revopush workflow - Coming Soon"
          echo "=================================="